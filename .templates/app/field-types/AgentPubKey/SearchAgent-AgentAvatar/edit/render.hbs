{{#if initial_value_variable}}
<div style="display: flex; flex-direction: row;">
<agent-avatar .agentPubKey=${ {{initial_value_variable}} }></agent-avatar>
{{/if}}
<search-agent .fieldLabel=${msg("{{label}}")} @agent-selected=${(e: CustomEvent) => { {{variable_to_change}} = e.detail.agentPubKey; } }></search-agent>
{{#if initial_value_variable}}
</div>
{{/if}}
