<vaadin-date-time-picker .label=${msg("{{label}}")} {{#if variable_to_read}}.value=${new Date({{variable_to_read}} / 1000).toISOString()}{{/if}} @change=${(e: CustomEvent) => { {{variable_to_change}} = new Date((e.target as any).value).valueOf() * 1000;} }></vaadin-date-time-picker>

