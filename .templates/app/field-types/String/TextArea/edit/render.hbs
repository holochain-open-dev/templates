<mwc-textarea outlined .label=${msg("{{label}}")} {{#if variable_to_read}}.value=${ {{variable_to_read}} }{{/if}} @input=${(e: CustomEvent) => { {{variable_to_change}} = (e.target as any).value;} }></mwc-textarea>
