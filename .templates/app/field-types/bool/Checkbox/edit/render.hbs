<mwc-formfield .label=${msg("{{label}}")}>
  <mwc-checkbox {{#if variable_to_read}}.checked=${ {{variable_to_read}} }{{/if}} @input=${(e: CustomEvent) => { {{variable_to_change}} = (e.target as any).checked;} }></mwc-checkbox>
</mwc-formfield>
