{{#merge_scope previous_file_content "devShells.default = pkgs.mkShell {"}}
              inputsFrom = [ inputs.holochain.devShells.${system}.holonix ];
              packages = [
                pkgs.nodejs-18_x
                pkgs.cargo-nextest
              ];
{{/merge}}
