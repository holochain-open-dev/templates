import { 
  collectionStore, 
  liveLinksStore, 
  deletedLinksStore, 
  latestVersionOfEntryStore, 
  immutableEntryStore, 
  deletesForEntryStore, 
  AsyncReadable,
  pipe
} from "@holochain-open-dev/stores";
import { slice, retype, EntryRecord, LazyHoloHashMap } from "@holochain-open-dev/utils";
import { NewEntryAction, Record, ActionHash, EntryHash, AgentPubKey } from '@holochain/client';

import { {{pascal_case app_name}}Client } from './{{kebab_case app_name}}-client.js';

export class {{pascal_case app_name}}Store {

  constructor(public client: {{pascal_case app_name}}Client) {}
  
}
