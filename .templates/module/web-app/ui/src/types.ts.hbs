import { 
  Record, 
  ActionHash, 
  DnaHash,
  SignedActionHashed,
  EntryHash, 
  AgentPubKey,
  Create,
  Update,
  Delete,
  CreateLink,
  DeleteLink
} from '@holochain/client';
import { ActionCommittedSignal } from '@holochain-open-dev/utils';

export type {{pascal_case app_name}}Signal = ActionCommittedSignal<EntryTypes, LinkTypes>;

export type EntryTypes = {};

export type LinkTypes = string;
