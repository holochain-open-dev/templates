import { Meta } from '@storybook/addon-docs';
import { ArgsTable } from '@storybook/addon-docs/blocks';
import { ProfilesStore } from '@holochain-open-dev/{{kebab_case app_name}}'

<Meta title="Frontend/{{pascal_case app_name}}Store" 
  parameters={ { previewTabs: { canvas: { hidden: true } } } }
/>

# {{pascal_case app_name}}Store

The `{{pascal_case app_name}}Store` is a typescript class that contains `svelte` stores, to which you can subscribe to get reactive updates in your elements.

```js
import { {{pascal_case app_name}}Store, {{pascal_case app_name}}Client } from "@holochain-open-dev/profiles";

const store = new {{pascal_case app_name}}Store(new {{pascal_case app_name}}Client(appAgentClient, 'my-role-name'));
```

> Learn how to setup the `AppAgentClient` object [here](https://www.npmjs.com/package/@holochain/client).

Learn more about the stores and how to integrate them in different frameworks [here](https://holochain-open-dev.github.io/reusable-modules/frontend/using/#stores).
